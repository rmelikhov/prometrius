# Шаг 1: Запуск стенда

Наш стенд автоматически запустился и состоит из трех компонентов:

## Проверим статус сервисов:

```bash
docker-compose ps


## Проверим метрики приложения:

```bash
curl http://localhost:8080/actuator/prometheus | head -10

